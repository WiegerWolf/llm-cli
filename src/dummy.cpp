// -----------------------------------------------------------------------------
// Dummy source file
// This placeholder is compiled into each Phase-0 OBJECT library that currently
// has no real implementation files.  It satisfies CMake’s requirement that an
// OBJECT/STATIC target has at least one source file.  The symbol is unique to
// avoid duplicate-definition errors when linked multiple times.
// -----------------------------------------------------------------------------
int llm_migration_placeholder() { return 0; }